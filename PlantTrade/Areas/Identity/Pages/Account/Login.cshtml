@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}
<script>
    function showFunc() {
        var checked = document.getElementById("showPass").checked;
        if (checked) {
            $("#password").attr("type", "text");
        } else {
            $("#password").attr("type", "password");
        }
    };
</script>

<div class="container">
    <div class="text-center">
        <h1 class="display-4">Inloggen</h1>
    </div>
    <div class="row">
        <div class="mx-auto col-md-4 mt-5 mb-5">
            <section>
                <form id="account" method="post">
                    <div class="container">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="Input.UserName"></label>
                            <input asp-for="Input.UserName" class="form-control" />
                            <span asp-validation-for="Input.UserName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Input.Password"></label>
                            <input id="password" asp-for="Input.Password" class="form-control" />
                            <input type="checkbox" id="showPass" onclick="showFunc()"/> Show
                            <span asp-validation-for="Input.Password" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <div class="checkbox">
                                <label asp-for="Input.RememberMe">
                                    <input asp-for="Input.RememberMe" />
                                    @Html.DisplayNameFor(m => m.Input.RememberMe)
                                </label>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="d-flex justify-content-center">
                                <button type="submit" class="btn btn-primary">Log in</button>
                            </div>
                        </div>
                        <div class="form-group text-center">
                            <p>
                                <a id="forgot-password" asp-page="./ForgotPassword">Wachtwoord vergeten?</a>
                            </p>
                            <p>
                                <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registreren als nieuwe gebruiker</a>
                            </p>
                        </div>
                    </div>
                </form>
            </section>
        </div>

        <!--
        <div class="col-md-6 col-md-offset-2">
            <section>

                @{
                    <h4>Use another service to log in.</h4>
                    <hr />

                    if ((Model.ExternalLogins?.Count ?? 0) == 0)
                    {

                    }
                    else
                    {
                        <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                            <div>
                                <p>
                                    @foreach (var provider in Model.ExternalLogins)
                                    {
                                        <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                    }
                                </p>
                            </div>
                        </form>
                    }
                }something

            </section>
        </div>
        -->
    </div>
</div>
<div class="row">
    <div class="feedback-div">
        <a class="btn btn-primary-feedback mb-2" asp-controller="Feedback" asp-action="Feedback" asp-route-page="login pagina">Feedback</a>
    </div>
</div>
@section Scripts {

    <partial name="_ValidationScriptsPartial" />
}
